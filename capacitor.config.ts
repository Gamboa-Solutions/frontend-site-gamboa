import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frontend-site-gamboa/',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
